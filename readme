hello this is note cli 

note cli is a command line app

note cli will allow users to quickly write a note, and have it saved anywhere or to specified location

to use the program:

there are 3 commands at this time

new
view
help

new will allow you to make a new note : >>> app.exe new path note
view will allow the user to view a note: >>> app.exe view path
help will tell you this stuff over again: >>> app.exe help
